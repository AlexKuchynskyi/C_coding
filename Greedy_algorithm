/**
	 * The program gives minimal amount of coins according to the sum user input.
     * The program implements [GitHub Pages](https://pages.github.com/) link:https://en.wikipedia.org/wiki/Greedy_algorithm[Greedy algorithm] which determines 
     * (in this very case) minimum number of coins to give while making change. As an example: 36 cents using only coins 
     * with values {1, 5, 10, 20}. 
*/

# include <stdio.h>
# include <cs50.h>
# include <math.h>

int main(void)
{
    float chislo;
    do
    {
        printf("Please, input your amount of money:");
        chislo = GetFloat();
    }
    while (chislo < 0);
    
    int money = round(chislo * 100);
    
    int kol_vo;
    int ostatok;
    
    kol_vo = money / 25;
    ostatok = money % 25;
    
    kol_vo = kol_vo + ostatok / 10;
    ostatok = ostatok % 10;
    
    kol_vo = kol_vo + ostatok / 5;
    ostatok = ostatok % 5;
    
    kol_vo = kol_vo + ostatok / 1;
    
    printf("Number is: %d\n", kol_vo);
}
